[package]
name = "comparer"
version = "0.1.0"
edition = "2024"

[dependencies]
parser = { path = "../parser" }
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
